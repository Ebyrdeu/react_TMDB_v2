import {createStyles} from "@mantine/core";

export const useMovieBottomStyles = createStyles(() => ({
		castWrapper: {
			display: "flex",
			flexWrap: "wrap",
			justifyContent: "center",
		},
	}),
);