import {createStyles} from "@mantine/core";

export const useActorBottomStyles = createStyles(() => ({
		castWrapper: {
			display: "flex",
			flexWrap: "wrap",
			justifyContent: "center",
		},
	}),
);
